Game Event Calendar v35

Changes from v34:
- Unified every image-based TABLE frame to 16px rendering.
- Update/add/edit outer frames now use 16px as well.
- Top tab image tables are explicitly fixed to 16px.
- Removed the v34 horizontal width compensation that was only needed for the 32px update outer frame.
